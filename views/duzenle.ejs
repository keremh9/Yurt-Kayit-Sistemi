<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>Öğrenci Düzenle | Kilis Vakfı</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style> :root { --vakif-yesil: #2d5a27; } .navbar { background-color: var(--vakif-yesil) !important; } </style>
</head>
<body class="bg-light">
    <nav class="navbar navbar-dark shadow mb-4">
        <div class="container"><a class="navbar-brand" href="/">ÖĞRENCİ DÜZENLEME PANELİ</a></div>
    </nav>
    <div class="container">
        <div class="card p-4 shadow-sm border-0" style="border-radius: 15px;">
            <h3 class="border-bottom pb-2 text-success fw-bold">Kayıt Güncelle</h3>
            
            <form action="/duzenle/<%= seciliOgrenci.id %>" method="POST" class="mt-4">
                <div class="mb-3">
                    <label class="form-label fw-bold">Öğrenci Adı Soyadı</label>
                    <input type="text" name="isim" class="form-control" value="<%= seciliOgrenci.isim %>" required pattern="[A-Za-zÇçĞğİıÖöŞşÜü\s]+" title="Lütfen sadece harf giriniz.">
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Oda Numarası</label>
                    <select name="odaNo" class="form-control" required>
                        <option value="105" <%= seciliOgrenci.odaNo === '105' ? 'selected' : '' %>>Oda 105</option>
                        <option value="G-1" <%= seciliOgrenci.odaNo === 'G-1' ? 'selected' : '' %>>Giriş Kat - G1</option>
                        <option value="Z-3" <%= seciliOgrenci.odaNo === 'Z-3' ? 'selected' : '' %>>Zemin Kat - Z3</option>
                    </select>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Telefon Numarası</label>
                    <input type="tel" name="tel" class="form-control" value="<%= seciliOgrenci.tel %>" required pattern="[0-9]{10,11}" title="Lütfen 10-11 haneli geçerli bir numara giriniz.">
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-success flex-grow-1" style="background-color: var(--vakif-yesil);">Değişiklikleri Kaydet</button>
                    <a href="/" class="btn btn-secondary">İptal</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
