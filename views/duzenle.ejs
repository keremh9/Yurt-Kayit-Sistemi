<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>Öğrenci Düzenle | Kilis Vakfı</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style> 
        :root { --vakif-yesil: #2d5a27; } 
        .navbar { background-color: var(--vakif-yesil) !important; } 
        .card { border-radius: 15px; border: none; }
        .list-group-item.active { background-color: var(--vakif-yesil); border-color: var(--vakif-yesil); }
    </style>
</head>
<body class="bg-light">
    <nav class="navbar navbar-dark shadow mb-4">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">KİLİS VAKFI YÖNETİM PANELİ</a>
        </div>
    </nav>

    <div class="container">
        <div class="card p-5 shadow-sm">
            <div class="text-center mb-4">
                <img src="/img/logo.png" class="mx-auto mb-3" style="height: 100px;">
                <h2 class="text-success fw-bold">Öğrenci Bilgilerini Güncelle</h2>
                <div style="width: 60px; height: 3px; background: #ffc107; margin: 10px auto;"></div>
            </div>

            <div class="row mt-4">
                <div class="col-md-4 border-end">
                    <h5 class="text-secondary fw-bold mb-3">Kayıtlı Öğrenciler</h5>
                    <div class="list-group shadow-sm">
                        <% if (ogrenciler && ogrenciler.length > 0) { %>
                            <% ogrenciler.forEach(o => { %>
                                <a href="/duzenle/<%= o.id %>" 
                                   class="list-group-item list-group-item-action <%= ogrenci && ogrenci.id === o.id ? 'active' : '' %>">
                                    <%= o.isim %>
                                </a>
                            <% }) %>
                        <% } else { %>
                            <p class="text-muted p-2">Listelenecek öğrenci yok.</p>
                        <% } %>
                    </div>
                </div>

                <div class="col-md-8 ps-md-5">
                    <% if (ogrenci) { %>
                        <h5 class="text-success fw-bold mb-4"><%= ogrenci.isim %> - Kaydını Düzenle</h5>
                        <form action="/duzenle/<%= ogrenci.id %>" method="POST">
                            <div class="mb-3 text-start">
                                <label class="form-label fw-bold small">Ad Soyad</label>
                                <input type="text" name="isim" class="form-control" value="<%= ogrenci.isim %>" required>
                            </div>

                            <div class="mb-3 text-start">
                                <label class="form-label fw-bold small">Oda Numarası</label>
                                <input type="text" name="odaNo" class="form-control" value="<%= ogrenci.odaNo %>" required>
                            </div>

                            <div class="mb-3 text-start">
                                <label class="form-label fw-bold small">Telefon Numarası</label>
                                <input type="tel" name="tel" class="form-control" value="<%= ogrenci.tel %>" required>
                            </div>

                            <div class="d-grid gap-2 mt-4">
                                <button type="submit" class="btn btn-success py-2 fw-bold" style="background-color: var(--vakif-yesil);">GÜNCELLEMELERİ KAYDET</button>
                                <a href="/" class="btn btn-outline-secondary">İşlemi İptal Et ve Dön</a>
                            </div>
                        </form>
                    <% } else { %>
                        <div class="alert alert-info text-center mt-4">
                            Lütfen düzenlemek istediğiniz öğrenciyi soldaki listeden seçiniz.
                        </div>
                    <% } %>
                </div>
            </div>

            <hr class="mt-5">
            <div class="text-center text-muted small">
                <strong>Sistem Yöneticisi:</strong> Mehmet Kerem Hakan (23040301045)
            </div>
        </div>
    </div>
</body>
</html>
