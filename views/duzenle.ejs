<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>Kayıt Yönetimi | Kilis Vakfı</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>:root { --vakif-yesil: #2d5a27; } .navbar { background-color: var(--vakif-yesil) !important; border-bottom: 5px solid #fbc02d; }</style>
</head>
<body class="bg-light">
    <nav class="navbar navbar-dark shadow mb-4">
        <div class="container"><a class="navbar-brand fw-bold" href="/">ÖĞRENCİ BİLGİ GÜNCELLEME</a></div>
    </nav>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-7">
                <div class="card shadow-lg p-4">
                    <label class="form-label fw-bold text-success">1. Güncellenecek Öğrenciyi Seçin</label>
                    <select class="form-select mb-4 bg-light" onchange="location = '/duzenle/' + this.value;">
                        <option value="" disabled <%= !seciliOgrenci ? 'selected' : '' %>>-- Kayıtlı Öğrenciler --</option>
                        <% ogrenciler.forEach(o => { %>
                            <option value="<%= o.id %>" <%= seciliOgrenci && seciliOgrenci.id == o.id ? 'selected' : '' %>><%= o.isim %> (Oda: <%= o.odaNo %>)</option>
                        <% }) %>
                    </select>

                    <% if (seciliOgrenci) { %>
                        <hr>
                        <h5 class="mb-3">Seçili Kişi: <span class="text-primary"><%= seciliOgrenci.isim %></span></h5>
                        <form action="/duzenle/<%= seciliOgrenci.id %>" method="POST">
                            <div class="mb-3">
                                <label class="form-label fw-bold">Tam Ad Soyad</label>
                                <input type="text" name="isim" class="form-control" value="<%= ogrenci.isim %>" required pattern="[A-Za-zÇçĞğİıÖöŞşÜü\s]+" title="Lütfen sadece harf giriniz."> 
                            </div>
                            <div class="mb-3">
                                <label class="form-label fw-bold">Yeni Oda Seçin</label>
                                <select name="odaNo" class="form-select" required>
                                    <option value="<%= seciliOgrenci.odaNo %>">Mevcut: <%= seciliOgrenci.odaNo %></option>
                                    <option value="Z-3">Z-3 (Bodrum)</option>
                                    <option value="G-1">G-1 (Giriş)</option>
                                    <% ["1", "2", "3"].forEach(kat => { %>
                                        <% ["01", "02", "03", "04", "05", "06", "07"].forEach(no => { %>
                                            <option value="<%= kat + no %>"><%= kat + no %></option>
                                        <% }) %>
                                    <% }) %>
                                </select>
                            </div>
                            <div class="mb-3">
                                <label class="form-label fw-bold">Öğrenci Telefon Numarası</label>
                                <input type="tel" name="telefon" class="form-control" value="<%= ogrenci.telefon %>" required pattern="[0-9]{10,11}" title="Lütfen 10-11 haneli geçerli bir numara giriniz.">
                            </div>
                            <div class="d-grid gap-2">
                                <button type="submit" class="btn btn-warning fw-bold">Değişiklikleri Kaydet</button>
                                <a href="/" class="btn btn-outline-secondary">Vazgeç</a>
                            </div>
                        </form>
                    <% } else { %>
                        <div class="alert alert-info text-center">Lütfen işlem yapmak için yukarıdan bir öğrenci seçin.</div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</body>
</html>