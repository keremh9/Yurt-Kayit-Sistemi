<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>Kilis Vakfı Yurdu | Yönetim</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root { --vakif-yesil: #2d5a27; }
        body { background-color: #f8f9fa; }
        .navbar { background-color: var(--vakif-yesil) !important; border-bottom: 5px solid #fbc02d; }
        .logo-img { height: 80px; background: white; border-radius: 50%; padding: 5px; }
        .room-card { width: 130px; margin: 8px; border-radius: 12px; border: 2px solid #ddd; }
        .bg-empty { border-color: #28a745 !important; background: #fff; }
        .bg-full { border-color: #dc3545 !important; background: #f8d7da; }
        .bg-available { border-color: #ffc107 !important; background: #fff3cd; }
        .floor-section { background: white; padding: 20px; border-radius: 15px; margin-bottom: 20px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark shadow-sm mb-4" style="background-color: #2d5a27; border-bottom: 4px solid #fbc02d;">
    <div class="container d-flex flex-column align-items-center py-2">
        
        <a href="/">
            <img src="/img/logo.png" alt="Kilis Vakfı Logo" 
                 style="height: 110px; width: 110px; object-fit: cover; border-radius: 50%; border: 2px solid rgba(255,255,255,0.2); background-color: white;">
        </a>
        
        <a class="navbar-brand fw-bold m-0 mt-2 text-center" href="/" style="font-size: 1.4rem; letter-spacing: 0.5px;">
            ALEV YAŞAR AKTÜRK KİLİS VAKFI YURDU
        </a>
        
        <div class="navbar-nav flex-row gap-0 mt-2">
            <a class="nav-link text-white fw-bold px-3 border-end border-light" style="font-size: 0.85rem;" href="/">Anasayfa</a>
            <a class="nav-link text-white fw-bold px-3 border-end border-light" style="font-size: 0.85rem;" href="/yeni-kayit">Yeni Kayıt</a>
            
            <a class="nav-link text-white fw-bold px-3 border-end border-light" style="font-size: 0.85rem;" href="/duzenle">Düzenle</a>
            
            <a class="nav-link text-white fw-bold px-3 border-end border-light" style="font-size: 0.85rem;" href="/hakkimizda">Hakkımızda</a>
            <a class="nav-link text-white fw-bold px-3" style="font-size: 0.85rem;" href="/iletisim">İletişim</a>
        </div>
    </div>
</nav>

    <div class="container">
        <% const katlar = [
            { ad: "Bodrum Kat", odalar: ["Z-3"] },
            { ad: "Giriş Kat", odalar: ["G-1"] },
            { ad: "1. Kat", odalar: ["101", "102", "103", "104", "105", "106", "107"] },
            { ad: "2. Kat", odalar: ["201", "202", "203", "204", "205", "206", "207"] },
            { ad: "3. Kat", odalar: ["301", "302", "303", "304", "305", "306", "307"] }
        ]; %>

        <% katlar.forEach(kat => { %>
            <div class="floor-section">
                <h5 class="text-success fw-bold"><%= kat.ad %></h5>
                <div class="d-flex flex-wrap">
                    <% kat.odalar.forEach(odaNo => { 
                        const kapasite = odaKapasitesi(odaNo);
                        const sakinler = ogrenciler.filter(o => o.odaNo === odaNo);
                        const doluluk = sakinler.length;
                        let renk = doluluk >= kapasite ? "bg-full" : (doluluk > 0 ? "bg-available" : "bg-empty");
                    %>
                        <div class="card room-card <%= renk %> shadow-sm">
                            <div class="card-body p-2 text-center">
                                <h6 class="fw-bold mb-1"><%= odaNo %></h6>
                                <p class="small m-0"><%= doluluk %> / <%= kapasite %></p>
                                <button class="btn btn-sm btn-outline-dark mt-2 border-0" 
                                        onclick="odaGoster('<%= odaNo %>', '<%= sakinler.map(s => s.isim).join(',') %>')">Detay</button>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        <% }); %>

        <div class="card shadow-sm mb-5">
    <div class="card-header bg-dark text-white fw-bold">Öğrenci Yönetimi</div>
    <div class="card-body p-0">
        <table class="table table-hover m-0">
            <thead class="table-light">
                <tr>
                    <th>Öğrenci Adı</th>
                    <th>Oda No</th>
                    <th>Öğrenci Telefon No</th> <th class="text-center">İşlemler</th>
                </tr>
            </thead>
            <tbody>
                <% ogrenciler.forEach(o => { %>
                    <tr>
                        <td class="align-middle"><%= o.isim %></td>
                        <td class="align-middle"><%= o.odaNo %></td>
                        <td class="align-middle"><%= o.tel %></td> <td class="text-center">
                            <a href="/duzenle">Düzenle</a> veya <a href="/duzenle/<%= ogrenci.id %>">Düzenle</a>
                            <a href="/sil/<%= o.id %>" class="btn btn-sm btn-danger fw-bold" 
                               onclick="return confirm('Bu kaydı silmek istediğinize emin misiniz?')">Sil</a>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</div>

    <div class="modal fade" id="odaModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content shadow-lg border-0">
                <div class="modal-header bg-success text-white">
                    <h5 class="modal-title" id="mBaslik"></h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body" id="mIcerik"></div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function odaGoster(no, isimler) {
            document.getElementById('mBaslik').innerText = 'Oda ' + no + ' Sakinleri';
            const icerik = document.getElementById('mIcerik');
            icerik.innerHTML = isimler ? isimler.split(',').map(i => `<div class="p-2 border-bottom">👤 ${i}</div>`).join('') : 'Oda şu an boş.';
            new bootstrap.Modal(document.getElementById('odaModal')).show();
        }
    </script>
</body>

</html>
